{
  "name": "askhat.io",
  "version": "1.0.0",
  "description": "Askhat's Homepage",
  "author": "Askhat Bikmetov <lolborgini@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "deploy": "npm run generate && push-dir --dir=dist --remote=gh-pages --branch=master --cleanup"
  },
  "dependencies": {
    "@nuxtjs/yandex-metrika": "^0.1.0",
    "nuxt": "^1.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^4.15.0",
    "eslint-friendly-formatter": "^3.0.0",
    "eslint-loader": "^1.7.1",
    "eslint-plugin-vue": "^4.0.0",
    "pug": "^2.0.3",
    "pug-loader": "^2.4.0",
    "push-dir": "^0.4.1"
  }
}
